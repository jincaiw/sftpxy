version: '3.8'

services:
  sftpxy:
    build: .
    container_name: sftpxy
    ports:
      - "2022:2022"   # SSH/SFTP
      - "2121:2121"   # FTP
      - "8080:8080"   # HTTP (WebAdmin/WebClient/API)
      - "8081:8081"   # WebDAV
      - "9090:9090"   # Telemetry/Metrics
    volumes:
      - ./config.yaml:/etc/sftpxy/config.yaml:ro
      - sftpxy-data:/data/sftpxy
      - sftpxy-logs:/var/log/sftpxy
    environment:
      - SFTPXY_DATA_PROVIDER_CONNECTION_STRING=/data/sftpxy/sftpxy.db
      - SFTPXY_COMMON_LOG_LEVEL=info
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  # Optional: MySQL database
  # mysql:
  #   image: mysql:8.0
  #   container_name: sftpxy-mysql
  #   environment:
  #     MYSQL_ROOT_PASSWORD: rootpassword
  #     MYSQL_DATABASE: sftpxy
  #     MYSQL_USER: sftpxy
  #     MYSQL_PASSWORD: sftpxypassword
  #   volumes:
  #     - mysql-data:/var/lib/mysql
  #   ports:
  #     - "3306:3306"
  #   restart: unless-stopped

volumes:
  sftpxy-data:
  sftpxy-logs:
  # mysql-data:
